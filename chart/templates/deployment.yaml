apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: "{{.Release.Name}}"
    app.kubernetes.io/component: server
  name: "{{.Release.Name}}-server"
  namespace: {{.Release.Namespace}}
spec:
  selector:
    matchLabels:
      app: "{{.Release.Name}}"
      app.kubernetes.io/component: server
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: "{{.Release.Name}}"
        app.kubernetes.io/component: server
    spec:
      containers:
        - image: "ghcr.io/arikkfir/traefik-google-oidc-auth:{{.Chart.AppVersion | replace "+" "_"}}"
          name: server
          env:
            - name: "CLIENT_ID"
              value: "{{.Values.oauth.clientId}}"
            - name: "CLIENT_SECRET"
              valueFrom:
                secretKeyRef:
                  name: "{{.Values.oauth.clientSecret.secretName}}"
                  key: "{{.Values.oauth.clientSecret.key}}"
            - name: "COOKIE_NAME"
              value: "{{.Values.cookieName}}"
            - name: "CSRF_COOKIE_NAME"
              value: "{{.Values.security.csrfCookieName}}"
            - name: "SCOPES"
              value: "{{.Values.oauth.scopes}}"
            - name: "CALLBACK_HOST"
              value: "{{.Values.oauth.callbackHost}}"
          ports:
            - containerPort: 8000
              name: http
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
      serviceAccountName: "{{.Release.Name}}-server"
